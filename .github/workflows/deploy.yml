name: Deploy React App via SFTP

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Install dependencies
      run: npm install

    - name: Build project
      run: npm run build

    - name: Deploy via SFTP
      uses: appleboy/scp-action@v0.1.5
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: '2222'  # استفاده از پورت 2222 برای SFTP
        source: "./dist/*"  # یا هر دایرکتوری دیگری که فایل‌های بیلد شده در آن هستند
        target: ${{ secrets.SERVER_PATH }}  # مسیر مقصد روی سرور شما
